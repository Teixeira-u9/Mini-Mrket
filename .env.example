# Backend "enchufable" — cuando exista la API real, define esta variable y
# la capa de servicios (src/lib/services) cambiará de mock a HTTP automáticamente.
NEXT_PUBLIC_API_BASE_URL=
# País del mercado (define moneda y métodos de pago). Soportado: CO, MX, AR.
NEXT_PUBLIC_COUNTRY=CO
